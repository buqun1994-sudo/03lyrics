interface:
  display_name: "任务收尾"
  short_description: "自动安装车机并完成最简验证、交接与提交门禁"
  default_prompt: "Use $task-closeout to install the debug build, run the minimum vehicle smoke, hand off user testing, and enforce the commit gate."
