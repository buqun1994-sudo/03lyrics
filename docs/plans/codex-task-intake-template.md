# Codex 任务入口模板

新对话已有 `AGENTS.md` 和项目文档时，不需要重复粘贴全部背景。复杂任务可使用以下最小输入：

```text
目标：
当前现象：
允许修改范围：
不能碰的范围：
完成标准：
实机或发布授权：
```

## 缺字段处理

1. 查询、说明、小文案和边界清晰的低风险任务可以从现有项目真值合理推断并推进。
2. 涉及包名、签名、权限、车辆接口、实机写操作、发布或破坏性动作时，缺少范围或授权必须先确认。
3. 用户明确要求只分析、只出方案、review 或不改文件时，不得施工。
4. 任务完成标准必须同时包含用户可观察结果和相应验证入口。
